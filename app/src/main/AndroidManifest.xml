<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="maxwell.vex.maxmart">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/max"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProductDetail"></activity>
        <activity android:name=".admin.AdminFashionProduct" />
        <activity android:name=".admin.AdminVideoGamesProducts" />
        <activity android:name=".admin.AdminAddProduct" />
        <activity android:name=".admin.AdminPanel" />
        <activity android:name=".admin.AdminLogin" />
        <activity android:name=".Home" />
        <activity android:name=".Registery" />
        <activity android:name=".OTP" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>